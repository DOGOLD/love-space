version: '3.8'

services:
  love-space:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    restart: always
    environment:
      - NODE_ENV=production